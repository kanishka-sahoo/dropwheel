# SwiftPM
.build/
.swiftpm/
Package.resolved

# App bundle, icons, release artifacts
build/

# Xcode (not used, but in case someone opens the package)
*.xcodeproj
*.xcworkspace
xcuserdata/
DerivedData/

# macOS
.DS_Store
